<?xml version="1.0" encoding="utf-8"?><!--  Toast进入动画  -->
<set xmlns:android="http://schemas.android.com/apk/res/android">

    <!--渐隐(显)  fromAlpha:开始时透明度    toAlpha： 结束时透明度    duration：动画持续时间 -->
    <alpha
        android:duration="300"
        android:fromAlpha="0.3"
        android:toAlpha="1"/>

    <!--平移  %p指相对于父容器 -->
    <!-- fromYDelta: Y轴方向开始位置，可以是%，也可以是具体的像素
         toYDelta:    Y轴方向结束位置，可以是%，也可以是具体的像素 -->
    <translate
        android:duration="300"
        android:fromYDelta="100%p"
        android:toYDelta="0" />

    <!-- 缩放
        interpolator             指定动画插入器，常见的有
            （开始时速度比较慢，在中间的时加速         accelerate_decelerate_interpolator
            加速插入器    accelerate_interpolator，减速插入器    decelerate_interpolator。）

        duration，               一次动画效果消耗的时间，单位毫秒，值越小动画速度越快
        fromXScale,fromYScale，  动画开始前X,Y的缩放，0.0为不显示，  1.0为正常大小
        toXScale，toYScale，     动画最终缩放的倍数， 1.0为正常大小，大于1.0放大
        pivotX，  pivotY         动画起始位置，相对于屏幕的百分比,两个都为50%表示动画从屏幕中间开始
        startOffset，            动画多次执行的间隔时间，如果只执行一次，执行前会暂停这段时间，单位毫秒
        repeatCount，            动画重复的计数，动画将会执行该值+1次
        repeatMode，             动画重复的模式，reverse为反向，当第偶次执行时，动画方向会相反。
        restart                  为重新执行，方向不变 -->

    <!--<scale-->
        <!--android:duration="1500"-->
        <!--android:fromXScale="0.3"-->
        <!--android:fromYScale="0.3"-->
        <!--android:pivotX="50%"-->
        <!--android:pivotY="50%"-->
        <!--android:toXScale="1.0"-->
        <!--android:toYScale="1.0"/>-->

    <!-- 旋转
  fromDegrees   动画开始时的角度
  toDegrees     动画结束时物件的旋转角度,正代表顺时针
  pivotX    属性为动画相对于物件的X坐标的开始位置
  pivotY    属性为动画相对于物件的Y坐标的开始位置    -->
    <!--<rotate-->
    <!--android:duration="10000"-->
    <!--android:fromDegrees="300"-->
    <!--android:interpolator="@android:anim/accelerate_decelerate_interpolator"-->
    <!--android:pivotX="10%"-->
    <!--android:pivotY="100%"-->
    <!--android:toDegrees="-360" />-->
</set>
